# App
APP_PORT=8001
APP_SECRET_KEY=changeme-generate-with-openssl-rand-hex-32
ENVIRONMENT=development

# Database
DATABASE_URL=postgresql+asyncpg://miningbible:password@localhost:5432/miningbible

# Claude (answer model)
ANTHROPIC_API_KEY=
CLAUDE_MODEL=claude-opus-4-8

# Embeddings (Voyage AI — free tier at voyageai.com)
VOYAGE_API_KEY=
EMBEDDING_MODEL=voyage-law-2
EMBEDDING_DIM=1024

# Stripe
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_PRICE_ID=

# Email (SMTP)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
EMAIL_FROM=

# Frontend (set to production domain in prod)
VITE_API_BASE_URL=http://localhost:8001

# CORS origins (comma-separated)
CORS_ORIGINS=http://localhost:5173,http://localhost:8001,https://mineready.io
